---
phase: 02-route
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/slack.py
  - tests/test_slack_post.py
autonomous: true

must_haves:
  truths:
    - "post_recap sends blocks to the correct Slack channel via chat.postMessage"
    - "A successful Slack API response (ok=true) does not raise"
    - "A Slack API error response (ok=false) raises RuntimeError with the error code"
    - "A not_in_channel error is surfaced as RuntimeError containing 'not_in_channel'"
    - "HTTP errors from requests raise (raise_for_status is called)"
  artifacts:
    - path: "app/slack.py"
      provides: "post_recap function"
      exports: ["post_recap"]
    - path: "tests/test_slack_post.py"
      provides: "Unit tests for post_recap with mocked requests.post"
      contains: "def test_"
  key_links:
    - from: "app/slack.py"
      to: "https://slack.com/api/chat.postMessage"
      via: "requests.post"
      pattern: "requests\\.post.*chat\\.postMessage"
---

<objective>
Implement app/slack.py — a thin function that posts pre-built Block Kit blocks to a Slack channel — and unit tests with mocked requests.

Purpose: Posting logic is isolated and testable independently of routing and webhook handling.
Output: app/slack.py with post_recap, and tests/test_slack_post.py with all cases passing.
</objective>

<execution_context>
@/Users/darren.rooney/.claude/flow/workflows/execute-plan.md
@/Users/darren.rooney/.claude/flow/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-route/02-RESEARCH.md
@app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement app/slack.py poster function</name>
  <files>app/slack.py</files>
  <action>
    Create app/slack.py with a single function post_recap(blocks: list, channel_id: str, bot_token: str) -> None.

    Follow the existing chat.postMessage pattern from app/main.py exactly (confirmed working):
    - POST to "https://slack.com/api/chat.postMessage"
    - Headers: Authorization: Bearer {bot_token}, Content-Type: application/json
    - JSON body: {"channel": channel_id, "blocks": blocks, "text": "Meeting Recap"}
    - Call response.raise_for_status() first (handles HTTP 4xx/5xx)
    - Parse response.json(), check data.get("ok")
    - If not ok: raise RuntimeError(f"Slack API error: {data.get('error')}")

    Do NOT copy DailyCoach.post_to_slack from main.py — that function contains daily-coach-specific fields (voting buttons, title_prefix). post_recap only needs to send pre-built blocks.

    No module-level state. No imports beyond requests.
  </action>
  <verify>python -c "from slack import post_recap; print('import ok')" works from app/ directory</verify>
  <done>app/slack.py exists and post_recap is importable with correct signature</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for post_recap</name>
  <files>tests/test_slack_post.py</files>
  <action>
    Create tests/test_slack_post.py. Import post_recap as: from slack import post_recap (conftest.py adds app/ to sys.path).

    Mock requests.post using monkeypatch or pytest-mock (pytest-mock is already a dev dependency). Mock at the boundary: patch "slack.requests.post".

    Write these test cases:
    1. test_post_recap_calls_correct_url — asserts requests.post called with "https://slack.com/api/chat.postMessage"
    2. test_post_recap_sends_channel_and_blocks — asserts json kwarg contains channel_id and blocks
    3. test_post_recap_ok_response_does_not_raise — mock returns {"ok": True, "ts": "123"}, no exception raised
    4. test_post_recap_error_response_raises_runtime_error — mock returns {"ok": False, "error": "channel_not_found"}, assert RuntimeError raised containing "channel_not_found"
    5. test_post_recap_not_in_channel_raises_runtime_error — mock returns {"ok": False, "error": "not_in_channel"}, assert RuntimeError raised containing "not_in_channel"
    6. test_post_recap_http_error_raises — mock raise_for_status raises requests.HTTPError, assert it propagates

    Use function-based tests and plain assert statements (not unittest.TestCase). No class wrapper needed.
  </action>
  <verify>cd /Users/darren.rooney/Documents/benchmark/repositories/dev_claude/slack-coach && poetry run pytest tests/test_slack_post.py -v</verify>
  <done>All 6 tests pass. Each covers a distinct behavior of post_recap at the HTTP boundary.</done>
</task>

</tasks>

<verification>
cd /Users/darren.rooney/Documents/benchmark/repositories/dev_claude/slack-coach && poetry run pytest tests/test_slack_post.py -v
</verification>

<success_criteria>
All 6 test_slack_post.py tests pass. post_recap correctly delegates to chat.postMessage, surfaces API errors as RuntimeError, and propagates HTTP errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-route/02-02-SUMMARY.md`
</output>
